[project]
name = "bashman"
version = "0.1.0"
description = "Bare-bones Bashman MVP: FastAPI registry + CLI"
authors = [{name="You", email="you@example.com"}]

# Point at your markdown README in the project root:
readme = "README.md"

license = {text = "MIT"}
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "jinja2",
  "python-multipart",
  "typer",
  "httpx"
]
